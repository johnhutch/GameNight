<%= form_for(@night) do |f| %>
  <% if @night.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@night.errors.count, "error") %> prohibited this night from being saved:</h2>

      <ul>
      <% @night.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
